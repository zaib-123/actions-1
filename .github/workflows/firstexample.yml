name: my workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout Repo
              uses: actions/checkout@v4

            - name: welcome message
              run: echo "my first workflow"

            - name: list a file
              run: ls
              
            - name: read file
              run: cat README.md

            